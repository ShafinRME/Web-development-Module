function rectangularArea(length, height) {
    let area;
    area = length * height;
    return area;
}

let length = 78;
let height = 80;

let getArea = rectangularArea(length, height);

console.log(getArea);